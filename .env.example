# Set to use OpenAI instead of Ollama + sentence-transformers
# TEAMIFIED_OPENAI_API_KEY=sk-...

# Ollama model (default: qwen3:8b)
# OLLAMA_MODEL=llama3:8b

# Path to PDF (default: auto-detected in project root)
# PDF_PATH=path/to/document.pdf
